\inst setlocal (Immediate link, Subscript index, Value src)

\otherwise \{
  FunctionFrame *fr;
  int i;
  fr = get_lp(context);
  for (i = 0; i < link; i++) fr = fframe_prev(fr);
  fframe_locals_idx(fr, index) = src;
#ifdef USE_REMEMBERED_SET
  write_barrier(&fframe_locals_idx(fr, index), src);
#endif /* USE_REMEMBERED_SET */
\}
